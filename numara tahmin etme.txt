#include <iostream>


int main(){
    int guess ;


    srand(time(NULL));
    int num = (rand() % 6) + 1;

    do
    {
        std::cin>>guess;

    } while (guess != num);
    
    std::cout<<num;
    return 0;
}